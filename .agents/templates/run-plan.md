# Plan — {{TITLE}}

Exactly one item is `in_progress` at a time.

| # | Task | Status | Notes |
|---|---|---|---|
| 1 | | `pending` | |
| 2 | | `pending` | |

Status values: `pending` · `in_progress` · `done` · `blocked` · `dropped`

A `blocked` or `dropped` row states why in Notes. A row that disappears without explanation
is what makes a handoff useless.

## Verification

How each task will be proven done — the command that runs, the test that passes, the thing
observed. Not "check manually".
